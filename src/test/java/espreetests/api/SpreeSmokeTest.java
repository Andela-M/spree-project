package espreetests.api;

public class SpreeSmokeTest {

    /*private String adminPassword = "spree123"
    private String adminEmail = "spree@example.com"

    @Test
    void tokens_and_basic_calls() {


        // PLATFORM: client_credentials (JSON body)
        String platformToken = TokenClient.getPlatformToken("<client_id>", "<client_secret>");
        PlatformApi platform = new PlatformApi();
        Response users = platform.listUsers(platformToken);
        System.out.println("Platform /users status = " + users.statusCode());
        assertEquals(200, users.statusCode());

        // STOREFRONT: password grant (FORM body)
        String storefrontToken = TokenClient.getStorefrontToken("<customer_email>", "<customer_password>");
        StorefrontApi storefront = new StorefrontApi();
        Response products = storefront.listProducts(storefrontToken);
        System.out.println("Storefront /products status = " + products.statusCode());
        assertEquals(200, products.statusCode()); */

}
